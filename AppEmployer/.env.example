# Copiá este archivo a .env.local y completá con la IP local de la máquina
# que está corriendo el backend (backend/, Express). Ambos dispositivos deben
# estar en la misma red WiFi. .env.local NO se sube a git.

# Backend único (Express) — auth, empleados, perfiles, trabajos, verificación, ubicación
EXPO_PUBLIC_API_URL=http://IP_DEL_BACKEND:3000
